# Copy to .env.creatures. Keep secrets local; never commit that file.
TRIPO_API_KEY=
# Required only for new concepts. Use an existing model on your text backend.
CREATURE_AI_MODEL=
CREATURE_AI_URL=http://127.0.0.1:11434/v1/chat/completions
CREATURE_AI_KEY=
# Optional existing checkout, at the pinned revision in scripts/creature-pipeline/chimera.mjs.
# CHIMERA_SOURCE_DIR=C:/path/to/chimera_cards
# Optional exact origins. Local HTTP origins are already allowed.
# CREATURE_ALLOWED_ORIGINS=https://jchoxha.github.io
